package string_demo;

public class String {
    /**
     * 字符串
     * java中所有""字符串都是String类的对象
     * 1.字符串不可变，他们的值创建后不可被更改
     * 2.不可变，但是可被共享
     * 3.字符串效果上是字符数组（char[]）,但底层原理是字节数组（byte[]）
     */
    public String(){}

    public void def(){

    }
}
